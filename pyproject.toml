[project]
name = "self-evolving-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "typer>=0.26.7",
]

[dependency-groups]
dev = [
    "pytest>=9.1.0",
]

[tool.ruff.lint]
fixable = []
